#include <stdio.h>
int main()
{

int radius=0, area=0,pi=3.14159;
printf("Enter the radius of Circle:");
scanf("%d", &radius);
area =pi* radius * radius;
printf("\nArea of Circle : %d", area);

    return 0;
}
